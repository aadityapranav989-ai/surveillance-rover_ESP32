#pragma once

// Copy this file to src/secrets.h and set a real password.
// src/secrets.h is ignored by Git so the password never reaches GitHub.
//
// WPA2 needs 8-63 characters. Everyone who knows this password can drive the
// rover, so choose one that is not easy to guess.
#define ROVER_WIFI_PASSWORD "CHANGE_ME_8_OR_MORE_CHARS"
